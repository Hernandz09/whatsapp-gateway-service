# Puerto del servidor API
PORT=8080

# URL de conexión a Redis (para Docker)
REDIS_URL=redis://redis:6379

# Directorio para guardar las sesiones (para Docker)
SESSION_DIR=/data/sessions

# Delays para el Hito 2 (Rate-Limit) [cite: 75-77]
TEXT_DELAY_MS=3500
MEDIA_DELAY_MS_MIN=6000
MEDIA_DELAY_MS_MAX=9000